import { InputText } from './InputText';

export default {
  title: 'InputText',
  component: InputText,
}

const Template = (args) => <InputText {...args} />

export const Default = {
  args: {
  }
}