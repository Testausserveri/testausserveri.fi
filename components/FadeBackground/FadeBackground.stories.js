import { FadeBackground } from './FadeBackground';

export default {
  title: 'FadeBackground',
  component: FadeBackground,
}

const Template = (args) => <FadeBackground {...args} />

export const Default = {
  args: { 
    
  }
}
