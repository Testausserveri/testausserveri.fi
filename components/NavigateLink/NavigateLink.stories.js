import { NavigateLink } from './NavigateLink';

export default {
  title: 'NavigateLink',
  component: NavigateLink,
}

const Template = (args) => <NavigateLink {...args} />

export const Default = {
}
