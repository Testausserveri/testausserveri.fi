import { InputDiscord } from './InputDiscord';

export default {
  title: 'InputDiscord',
  component: InputDiscord,
}

const Template = (args) => <InputDiscord {...args} />

export const Default = {
  args: {
  }
}