import { Projects3D } from './Projects3D';

export default {
  title: 'Projects3D',
  component: Projects3D,
}

const Template = (args) => <Projects3D {...args} />

export const Default = {
  args: {
  }
}