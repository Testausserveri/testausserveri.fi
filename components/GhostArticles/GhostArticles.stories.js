import { GhostArticles } from './GhostArticles';

export default {
  title: 'GhostArticles',
  component: GhostArticles,
}

const Template = (args) => <GhostArticles {...args} />

export const Default = {
  args: {
    
  }
}
