import { AvatarRow } from './AvatarRow';

export default {
  title: 'AvatarRow',
  component: AvatarRow,
}

const Template = (args) => <AvatarRow {...args} />

export const Default = {
  args: {
    
  }
}