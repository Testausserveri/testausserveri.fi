import { COMPONENT } from './COMPONENT';

export default {
  title: 'COMPONENT',
  component: COMPONENT,
}

const Template = (args) => <COMPONENT {...args} />

export const Default = {
  args: {
    
  }
}
